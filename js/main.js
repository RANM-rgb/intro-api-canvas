const canvas = document.getElementById("canvas");
const ctx = canvas.getContext("2d");

// cuadro verde en la esquina superior izquierda del canvas
ctx.fillStyle = "green";
ctx.fillRect(10, 10, 100, 100);
